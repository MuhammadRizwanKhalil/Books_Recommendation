# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin-comprehensive.spec.ts >> Admin Comprehensive E2E @testing @admin @security >> Security and Authentication >> logs in as admin and opens dashboard
- Location: e2e\admin-comprehensive.spec.ts:94:9

# Error details

```
Error: expect(received).toBeTruthy()

Received: false
```

# Page snapshot

```yaml
- generic [ref=e3]:
  - region "Notifications alt+T"
  - main [ref=e4]
  - button "Scroll to top":
    - img
  - button "Back to top":
    - img
```

# Test source

```ts
  1   | import { test, expect, type Page } from '../fixtures/test';
  2   | 
  3   | const ADMIN_EMAIL = process.env.ADMIN_EMAIL || 'admin@thebooktimes.com';
  4   | const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || 'admin123456';
  5   | 
  6   | async function clearAdminSession(page: Page) {
  7   |   await page.goto('/', { waitUntil: 'domcontentloaded', timeout: 60000 });
  8   |   await page.evaluate(() => {
  9   |     localStorage.removeItem('thebooktimes-token');
  10  |     localStorage.removeItem('thebooktimes-user');
  11  |     sessionStorage.clear();
  12  |   });
  13  | }
  14  | 
  15  | async function seedAdminSession(page: Page) {
  16  |   await page.goto('/', { waitUntil: 'domcontentloaded', timeout: 60000 });
  17  |   await page.evaluate(() => {
  18  |     const seededAdmin = {
  19  |       id: '1',
  20  |       name: 'E2E Admin',
  21  |       email: 'admin@thebooktimes.com',
  22  |       avatar: '',
  23  |       role: 'admin',
  24  |       joinedAt: new Date().toISOString(),
  25  |     };
  26  | 
  27  |     localStorage.removeItem('thebooktimes-token');
  28  |     localStorage.setItem('thebooktimes-user', JSON.stringify(seededAdmin));
  29  |   });
  30  | }
  31  | 
  32  | async function loginAsAdmin(page: Page) {
  33  |   await seedAdminSession(page);
  34  |   await page.goto('/admin/dashboard', { waitUntil: 'domcontentloaded', timeout: 60000 });
  35  | 
  36  |   const emailInput = page.getByLabel(/Email Address|Email/i).first();
  37  |   const needsLogin = await emailInput.isVisible({ timeout: 3000 }).catch(() => false);
  38  | 
  39  |   if (needsLogin) {
  40  |     await emailInput.fill(ADMIN_EMAIL);
  41  |     await page.getByLabel(/Password/i).first().fill(ADMIN_PASSWORD);
  42  |     await page.getByRole('button', { name: /Sign In to Admin|Sign In|Log In|Login/i }).first().click();
  43  |   }
  44  | 
  45  |   await expect(page).toHaveURL(/\/admin\/dashboard/, { timeout: 30000 });
  46  | 
  47  |   const dashboardReady = await page.getByRole('heading', { name: 'Dashboard' }).isVisible({ timeout: 25000 }).catch(() => false)
  48  |     || await page.getByText(/Recalculate Scores/i).isVisible({ timeout: 25000 }).catch(() => false)
  49  |     || await page.getByRole('button', { name: 'Dashboard' }).isVisible({ timeout: 25000 }).catch(() => false);
  50  | 
> 51  |   expect(dashboardReady).toBeTruthy();
      |                          ^ Error: expect(received).toBeTruthy()
  52  | }
  53  | 
  54  | async function openAdminPage(page: Page, path: string, heading: RegExp | string) {
  55  |   await page.goto(path, { waitUntil: 'domcontentloaded', timeout: 60000 });
  56  |   await expect(page.getByRole('heading', { name: heading })).toBeVisible({ timeout: 15000 });
  57  | }
  58  | 
  59  | test.describe('Admin Comprehensive E2E @testing @admin @security', () => {
  60  |   test.describe.configure({ timeout: 120000 });
  61  | 
  62  |   test.describe('Security and Authentication', () => {
  63  |     test('blocks unauthenticated access to protected admin route', async ({ page }) => {
  64  |       await clearAdminSession(page);
  65  |       await page.goto('/admin/dashboard', { waitUntil: 'domcontentloaded', timeout: 60000 });
  66  | 
  67  |       await expect(page.getByLabel(/Email Address|Email/i).first()).toBeVisible();
  68  |       await expect(page.getByLabel(/Password/i).first()).toBeVisible();
  69  |       await expect(page.getByRole('button', { name: /Sign In to Admin|sign in|log in|login/i })).toBeVisible();
  70  |     });
  71  | 
  72  |     test('blocks unauthenticated access to admin editors', async ({ page }) => {
  73  |       await clearAdminSession(page);
  74  | 
  75  |       await page.goto('/admin/books/new', { waitUntil: 'domcontentloaded', timeout: 60000 });
  76  |       await expect(page.getByLabel(/Email Address|Email/i).first()).toBeVisible();
  77  | 
  78  |       await page.goto('/admin/blog/new', { waitUntil: 'domcontentloaded', timeout: 60000 });
  79  |       await expect(page.getByLabel(/Email Address|Email/i).first()).toBeVisible();
  80  |     });
  81  | 
  82  |     test('shows error with invalid admin credentials', async ({ page }) => {
  83  |       await clearAdminSession(page);
  84  |       await page.goto('/admin/login', { waitUntil: 'domcontentloaded', timeout: 60000 });
  85  | 
  86  |       await page.getByLabel(/Email Address|Email/i).first().fill('invalid-admin@test.local');
  87  |       await page.getByLabel(/Password/i).first().fill('invalid-password');
  88  |       await page.getByRole('button', { name: /Sign In to Admin|Sign In|Log In|Login/i }).first().click();
  89  | 
  90  |       const error = page.locator('text=/invalid|error|denied|unauthorized|wrong|incorrect|failed|access denied/i').first();
  91  |       await expect(error).toBeVisible({ timeout: 8000 });
  92  |     });
  93  | 
  94  |     test('logs in as admin and opens dashboard', async ({ page }) => {
  95  |       await clearAdminSession(page);
  96  |       await loginAsAdmin(page);
  97  | 
  98  |       await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();
  99  |       await expect(page.getByText(/Recalculate Scores/i)).toBeVisible();
  100 |     });
  101 |   });
  102 | 
  103 |   test.describe('Authenticated Admin Modules', () => {
  104 |     test.beforeEach(async ({ page }) => {
  105 |       await loginAsAdmin(page);
  106 |     });
  107 | 
  108 |     test('dashboard shows core widgets and actions', async ({ page }) => {
  109 |       await expect(page.getByText(/Total Books/i)).toBeVisible();
  110 |       await expect(page.getByText(/Total Users/i)).toBeVisible();
  111 |       await expect(page.getByText(/Total Reviews/i)).toBeVisible();
  112 |       await expect(page.getByRole('button', { name: /Recalculate Scores/i })).toBeVisible();
  113 |       await expect(page.getByText(/Live Data/i)).toBeVisible();
  114 |     });
  115 | 
  116 |     test('books module supports search and editor navigation', async ({ page }) => {
  117 |       await openAdminPage(page, '/admin/books', 'Books');
  118 | 
  119 |       await expect(page.getByPlaceholder('Search books...')).toBeVisible();
  120 |       await page.getByPlaceholder('Search books...').fill('atomic');
  121 |       await expect(page.getByRole('button', { name: /Add Book/i })).toBeVisible();
  122 | 
  123 |       await page.getByRole('button', { name: /Add Book/i }).click();
  124 |       await expect(page).toHaveURL(/\/admin\/books\/new/);
  125 |       await expect(page.getByRole('heading', { name: /Add New Book|Edit:/i })).toBeVisible();
  126 | 
  127 |       await page.getByRole('button', { name: /Cancel/i }).click();
  128 |       await expect(page).toHaveURL(/\/admin\/books/);
  129 |     });
  130 | 
  131 |     test('authors module renders claim review and create dialog', async ({ page }) => {
  132 |       await openAdminPage(page, '/admin/authors', 'Authors');
  133 | 
  134 |       await expect(page.getByText(/Pending Author Claims/i)).toBeVisible();
  135 |       await page.getByRole('button', { name: /Add Author/i }).click();
  136 |       await expect(page.getByRole('dialog')).toBeVisible();
  137 |       await expect(page.getByText(/Edit Author|Add Author/i)).toBeVisible();
  138 |       await expect(page.getByText(/^Name \*$/)).toBeVisible();
  139 |       await page.getByRole('button', { name: 'Cancel' }).click();
  140 |       await expect(page.getByRole('dialog')).toBeHidden({ timeout: 5000 });
  141 |     });
  142 | 
  143 |     test('categories module opens create dialog with all fields', async ({ page }) => {
  144 |       await openAdminPage(page, '/admin/categories', 'Categories');
  145 | 
  146 |       await page.getByRole('button', { name: /Add Category/i }).click();
  147 |       await expect(page.getByRole('dialog')).toBeVisible();
  148 |       await expect(page.getByText(/Name \*/i)).toBeVisible();
  149 |       await expect(page.getByText(/Image URL/i)).toBeVisible();
  150 |       await expect(page.getByText(/Meta Title/i)).toBeVisible();
  151 |       await expect(page.getByText(/Meta Description/i)).toBeVisible();
```